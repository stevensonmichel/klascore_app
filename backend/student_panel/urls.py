from django.urls import path
# from .views import (
#     StudentDashboardView, MyGradesView, SubmitComplaintView, RequestTranscriptView
# )

urlpatterns = [
    # path('dashboard/', StudentDashboardView.as_view()),
    # path('my-grades/', MyGradesView.as_view()),
    # path('submit-complaint/', SubmitComplaintView.as_view()),
    # path('request-transcript/', RequestTranscriptView.as_view()),
]